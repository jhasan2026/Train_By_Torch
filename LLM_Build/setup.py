from setuptools import setup, find_packages

setup(
    name='LLM_Build',
    version='0.1.0',
    description='A customized package for GPT-2 small Model.',
    author='Jehan Hasan',
    packages=find_packages(),
    python_requires='>=3.6',
)
